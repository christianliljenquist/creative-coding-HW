function setup() {
createCanvas(800, 400);
background(204);
}

function draw() {
  // put drawing code here
  ellipse(400, 200, 300, 140); // the body
  ellipse(400, 100, 50, 60); // the head
  ellipse(390, 100, 10, 10);
  ellipse(410, 100, 10, 10);
}
